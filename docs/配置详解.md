# 配置详解

本文说明当前代码实际生效的 runtime 配置。配置入口主要是 `default_orchestrator.py`：
顶层字段选择 Package、外部 runtime、workflow provider 和 LLM role 配置；`env` 承载运行时
参数和本地环境选择。

## 1. 配置加载

启动命令示例：

```powershell
python default_orchestrator.py --config runtime_config.camping.package.smoke.json
```

文档中的 `python` 表示用户自己的 Python 3.10+ 解释器；建议在已激活的虚拟环境中运行。

查找顺序：

1. `--config`
2. 未传 `--config` 时使用项目根目录下的 `runtime_config.json`

配置文件格式：

```json
{
  "packages": [
    {"path": "Packages/Camping", "world": true}
  ],
  "default_workflow_provider": {"kind": "simple"},
  "env": {}
}
```

`env` 中的值会转换为字符串后解析。真实本地配置可能包含 API key，应保持 ignored；仓库只提交脱敏示例。

当前 runtime config 顶层字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `packages` | 是 | Package 选择；必须恰好一个条目带 `world: true` |
| `default_workflow_provider` | 否 | 默认 Agent workflow；未设置时为 `{"kind": "simple"}` |
| `env` | 是 | 运行参数；所有值转成字符串 |
| `external_runtimes` | 否 | 外部 runtime 实例，例如 SQLite 社交平台 |
| `workflow_providers` | 否 | 额外注册的内置 workflow provider |
| `llm_providers` | LLM workflow 时需要 | LLM HTTP provider 配置 |
| `workflows` | LLM workflow 时需要 | 内置 `llm` workflow 的 role/model 配置 |
| `default_workflow_id` | LLM workflow 时需要 | 内置 `llm` workflow 的默认 profile ID |

未列出的顶层字段当前不会被 runtime 装配读取；不要把实验方法参数散落在 runtime config 中。
场景事实应放在 Package data / study config 中。

当前提交的 smoke 配置包括：

- `runtime_config.camping.package.smoke.json`：Camping 无 LLM smoke。

## 2. Package 与数据加载

`packages` 必须恰好选择一个 `world: true` 条目。世界数据来自该 Package 的
`kern-package.json` 中 `data.world`、`data.entities`、`data.recipes`、`data.reactions` 和
`data.bundles`；这些字段不再从 `env.WORLD_JSON`、`RECIPES_JSONS` 等旧配置读取。

能力 Package 可贡献 recipes、reactions、named bundles，以及通过 `extensions.py` 声明的
Effect、Component、codec 和 external runtime provider。loader 只导入入口列出的
`COMPONENT_MODULES`、`EFFECT_MODULES` 与 `EXTERNAL_RUNTIME_MODULES`，并将已标记定义注册到
本次 runtime 的 catalog。所有数据引用只在用户已经选择的 Package 集合内解析；loader 不根据
引用自动递归加载其他 Package。完整格式和 checkpoint identity 规则见 `current_architecture.md`。

## 3. 外部 runtime 字段

`external_runtimes` 是数组，每项格式：

```json
{
  "runtime_id": "social_platform",
  "provider": "social_propagation:sqlite_platform",
  "options": {
    "database_path": "KERN/external_runtimes/social_runs/consequence/platform.sqlite",
    "seed_path": "Packages/SeaLevelSocialExperiment/Data/Platform/social_seed.sea_level_consequence_focus.json"
  }
}
```

| 字段 | 说明 |
| --- | --- |
| `runtime_id` | 本次运行中的实例 ID；effect 通过该 ID 调用 |
| `provider` | 已选 Package 注册的外部 runtime provider ID |
| `options` | 纯 JSON 选项；由对应 provider 工厂解释 |

当前 Package loader 只允许使用已选 Package 明确注册的 provider。`options` 会进入 runtime
identity，不应直接放密钥。密钥应通过环境变量名或 ignored 本地配置间接提供。

## 4. Workflow 字段

workflow 都是源码内置实现。runtime config 只能选择已经在
`KERN.agent_workflow.builtin_workflows.BUILTIN_WORKFLOW_BUILDERS` 注册的 `kind`；不会从
config import 任意 Python 工厂，也不会自动发现 Package workflow。开发者要新增 workflow，
需要改源码注册表和对应 builder。

当前内置 kind：

| kind | 说明 |
| --- | --- |
| `simple` | 不使用 LLM 的简单策略；未显式选择默认 workflow 时使用 |
| `llm` | 默认 Planner/Grounder/Dialogue LLM workflow；使用顶层 `llm_providers`、`workflows` 和 `default_workflow_id` |
| `social_platform` | 社交平台单页决策 workflow；从世界 Package 的 study data 读取 schedule、actor bindings 和实验帖 ID |

默认 workflow 的选择：

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `default_workflow_provider.kind` | `simple` | 默认 workflow kind |
| `default_workflow_provider.options` | `{}` | 传给默认 workflow builder |
| `DEFAULT_WORKFLOW_KIND` | 空 | env 兼容入口；设置后覆盖 `default_workflow_provider.kind` |

额外 provider 用顶层 `workflow_providers` 声明：

```json
{
  "workflow_providers": [
    {"workflow_id": "social_platform", "kind": "social_platform"}
  ]
}
```

字段：

| 字段 | 说明 |
| --- | --- |
| `workflow_id` | 与实体 `AgentControlComponent.provider_id` 对应 |
| `kind` | 内置 workflow kind |
| `options` | 可选；传给该 kind 的源码 builder |

`social_platform` 当前支持的 `options` 只有运行层调优项：`model`、`temperature`、`max_tokens`、
`max_memory_items`、`identity_component_id`。schedule、actor bindings 和 experimental post ID
来自世界 Package 数据，不属于 runtime config。

`social_platform` 还读取以下 `env` 字段来构造 LLM client：

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `LLM_PROVIDER` | `openai_compat` | LLM 协议 |
| `LLM_BASE_URL` | 空 | API 根地址 |
| `LLM_API_PREFIX` | `/v1` | API 前缀 |
| `LLM_API_KEY` | 空 | 直接 API key；优先级高于 `LLM_API_KEY_ENV` |
| `LLM_API_KEY_ENV` | `LLM_API_KEY` | 从环境变量读取 API key |
| `LLM_TIMEOUT_SECONDS` | `60` | 单次请求超时 |
| `LLM_MAX_RETRIES` | `2` | 请求级重试次数 |
| `LLM_RETRY_BACKOFF_SECONDS` | `1.0` | 指数退避基准秒数 |
| `LLM_REQUEST_EXTRA_JSON` | 空 | 附加 provider 私有字段 |
| `SOCIAL_WORKFLOW_MODEL` | `LLM_PLANNER_MODEL` | 社交 workflow 模型名 |
| `SOCIAL_WORKFLOW_TEMPERATURE` | `0.2` | 社交 workflow 温度 |
| `SOCIAL_WORKFLOW_MAX_TOKENS` | `1200` | 社交 workflow 输出 token 上限 |

## 5. 运行循环字段

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `MAX_TICKS` | 空 | 世界 `total_ticks` 的绝对上限，不是“本次再推进多少 tick” |
| `MAX_TICKS_DEFAULT_LLM` | `15` | 默认 workflow 为 `llm` 且未设置 `MAX_TICKS` 时使用 |
| `MAX_TICKS_DEFAULT_NO_LLM` | `65` | 默认 workflow 不是 `llm` 且未设置 `MAX_TICKS` 时使用 |
| `MAX_TRIGGER_DEPTH` | `4` | reaction 层数上限；原始 event 为第 0 层，直接触发的 reaction 为第 1 层；超出上限抛出 `REACTION_DEPTH_EXCEEDED` 并终止 runtime |
| `MAX_ACTIONS_PER_TURN` | `99` | 一个 turn 最多提交的 Action 数量；最小有效值为 1 |
| `MAX_REPLANS_PER_TURN` | `5` | 一个 turn 因 rejection 重新规划的次数上限；最小有效值为 0 |
| `DIALOGUE_BUDGET_LIMIT_PER_LOCATION` | `4` | 每地点对话预算 |
| `WORKFLOW_VIEW_PROFILE` | `embodied_default` | workflow 视图裁剪策略 |
| `WORKFLOW_VIEW_PROFILE_JSON` | 空 | 自定义 workflow 视图配置 JSON；相对路径按 runtime config 所在目录解析，可覆盖内置 profile |

如果从 checkpoint 恢复，`MAX_TICKS` 仍按恢复后世界的 `total_ticks` 判断。比如恢复档已经在 tick 120，而 `MAX_TICKS=60`，`run(max_ticks)` 会记录初始状态后立即结束，不会再推进 60 tick。上层需要“从当前状态再推进 N tick”时，应使用 `advance_ticks(count)`。

Reaction 使用确定性的 FIFO event 队列：bundle 成功后按产生顺序发布 events，同一个 event 命中的 reactions 按配置顺序执行，新产生的 events 追加到队尾。每个 reaction bundle 是独立事务；前面已经成功提交的 reaction 会保留，当前失败 bundle 会回滚其 `WorldState` 写入。`InvokeBundle`、`RandomBundle`、`ApplyToQuery`、任务生命周期和 `WorkerTick` 引用的 child bundle 则属于所在父 bundle 的同一事务：child 失败会使父 bundle 失败，child events 只在最外层 bundle 成功后发布。Reaction 中出现任意 `BindError` 或 `ExecutorError` 都会记录 `ReactionFailed` 并立即中断模拟。外部 runtime 的写入不参与 `WorldState` 事务，仍受各 adapter 自身的一致性能力限制。

## 6. 日志配置

这些字段由 `default_orchestrator.py` 读取，用于控制运行时日志输出：

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `LOG_LEVEL` | `INFO` | 日志等级 |
| `LOG_CATEGORIES` | 空 | 逗号分隔的日志类别过滤 |
| `LOG_JSON` | `0` | 是否以 JSON 形式输出日志 |
| `LOG_BUFFER_SIZE` | `1000` | 内存日志缓冲大小 |

`KernRuntime` 是当前 KERN SDK 入口和 runtime runner，可通过 `from KERN import KernRuntime` 或 `from KERN.runtime import KernRuntime` 引入。

公开 runtime API：

- `record_initial_state()`：记录当前 tick，不推进时间。
- `step()`：推进一个 tick，返回该 tick 产生的 event log records；这是低层 API，不自动写 snapshot/checkpoint/log。
- `step_and_record()`：推进一个 tick，并记录 snapshot/checkpoint/log。
- `advance_ticks(count)`：上层 app/server 手动推进 runtime 的推荐 API。
- `run(max_ticks)`：批处理运行入口，会先记录初始状态，再循环 `step_and_record()`。

`KernRuntime.step()` 每 tick 会推进时间并结算 `WorldTickAdvanced`，再把按 Entity ID 排序的全部 `AdvanceTick` 放入同一个 FIFO。被动 Reaction 全部完成后，`KERN.sim.turn_scheduler.TurnScheduler` 才按稳定顺序检查受控 Entity；是否授予 turn 由 `AgentWakePolicyComponent` 和 interrupt rules 决定。

上层 app 不应直接调用 `_capture_snapshot`、`_save_checkpoint` 或 `_save_simulation_log` 这类 private 方法；需要手动推进时使用 `advance_ticks(count)`。

## 7. Workflow Contract

新 workflow 的调用约定：

```python
session = workflow.begin_turn(ws, turn_start)
step = session.next_step(ws, turn_frame)
```

`step` 必须是 `SubmitAction(intent)` 或 `EndTurn()`。`TurnFrame` 只包含 actor、唤醒原因、mode context、上一条 action feedback 和 turn 计数。workflow 直接读取当前 `ws` 并自行构造领域 perception，可以在 session 内维护多步计划和游标，但不能直接写 `WorldState`。

对话使用独立 `DialoguePolicy.decide_utterance(DialogueFrame)` interface，返回 `Speak(text)` 或
`Pass()`。`StartConversation` 负责稳定参与者顺序和预算，并把每句成功发言写成 interaction；
完整对话不依赖日志开关。

`RecordInteraction` 在交互发生时写入各 Agent 的 `PerceptionComponent.interaction_inbox`。具体 workflow 决定如何读取 inbox 和 memory；默认 LLM workflow 调用自己的 `memory_policy`，再通过 `ApplyMemoryPatch` 原子地写入正式记忆并消费 inbox。

执行约束：

- TurnRunner 用 `InteractionEngine` 把每条 ActionIntent 编译为 EffectBundle，完成 Reaction 结算后才请求下一步。
- 正常领域 rejection 作为 `ActionFeedback(status="rejected")` 返回 session。
- memory patch、provider、contract、Effect 或 Reaction 失败会触发 terminal `KernFailure`。
- 世界写入必须经 executor/effect，不应由 workflow 直接改 `WorldState`。

Workflow contract 当前固定为 fail-fast。旧的 `WORKFLOW_CONTRACT_ON_ERROR` 配置键已不再读取。

## 8. Agent 思考调度

当前实现把思考触发分成两层：

1. runtime 每 tick 提供调度脉冲。
2. agent 的 `AgentWakePolicyComponent` 和 interrupt rules 判断是否授予 turn。

这意味着 KERN 可以统一询问所有 agent，但不会强迫每个 agent 每 tick 都生成动作。场景数据可以通过 preset/rule 调整 agent 的警觉性、专注度和中断倾向。

常见规则实现位于：

- `KERN/agent_workflow/interrupt_rules/`
- `KERN/agent_workflow/interrupt_runtime.py`

`AgentWakePolicyComponent` 当前主要保存：

- `rules`
- `active_interrupt_preset_id`
- `interrupt_presets`
- `interrupt_preset_descriptions`
- `interrupt_runtime_state`

`NoActiveTask` 这类基础规则不受 preset 的 `enabled` 开关控制；preset 更适合调整长期任务中的中断倾向和部分规则参数。当前允许 agent 通过 `SwitchInterruptPreset` 切换 preset，但不开放直接 patch rule 参数的通用接口。

## 9. LLM 配置

LLM 运行时由三个概念组成：

- `llm_providers`：协议客户端。一个 provider 表示一个可请求的 HTTP API 端点，例如 OpenAI-compatible 或 Gemini。
- `workflows`：内置 `llm` workflow 的 profile 配置。一个 profile 必须声明 `planner`、`grounder`、`dialogue` 三个 role。
- role binding：workflow 内每个 role 绑定到一个 `provider_id` 和一个 `model`。同一个 workflow 可以让 planner、grounder、dialogue 使用不同 provider 或不同模型。

使用默认 LLM workflow 时，在 runtime config 顶层声明 `default_workflow_provider`、
`llm_providers`、`workflows`、
`default_workflow_id`：

```json
{
  "default_workflow_provider": {"kind": "llm"},
  "env": {"LLM_TRACE_MODE": "full"},
  "llm_providers": {
    "main": {
      "protocol": "openai_compat",
      "base_url": "https://example.test",
      "api_prefix": "/v1",
      "api_key_env": "KERN_LLM_API_KEY",
      "timeout_seconds": 60,
      "max_retries": 2,
      "retry_backoff_seconds": 1.0
    }
  },
  "workflows": {
    "default": {
      "kind": "llm",
      "roles": {
        "planner": {"provider_id": "main", "model": "planner-model"},
        "grounder": {"provider_id": "main", "model": "grounder-model"},
        "dialogue": {"provider_id": "main", "model": "dialogue-model"}
      }
    }
  },
  "default_workflow_id": "default"
}
```

`llm_providers` 字段：

| 字段 | 说明 |
| --- | --- |
| `protocol` | `openai_compat`、`openai_compatible`、`openai_chat`、`openai`、`gemini` 或 `gemini_generate_content` |
| `base_url` | API 根地址 |
| `api_prefix` | API 前缀，例如 `/v1` 或 `/v1beta` |
| `api_key` / `api_key_env` | 二选一；建议使用 `api_key_env` |
| `timeout_seconds` | 单次请求超时，默认 `60` |
| `max_retries` | 请求级重试次数，默认 `2` |
| `retry_backoff_seconds` | 指数退避基准秒数，默认 `1.0` |

role binding 可选 `request_extra`，用于给该 role 的请求附加 provider 私有字段：

```json
"planner": {
  "provider_id": "main",
  "model": "planner-model",
  "request_extra": {"seed": 1}
}
```

workflow 可选 `options`：

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `debug` | `false` | 是否启用 LLM debug 日志 |
| `focus_agent_id` | 空 | 聚焦某个 agent 的日志 |
| `focus_log_prompts` | `false` | 是否输出 prompt 日志 |
| `focus_log_perception` | `true` | 是否输出感知日志 |
| `llm_debug_view` | 空 | 逗号分隔的 planner 映射字段过滤列表；仅在 debug/trace 日志路径启用时生效 |

Trace 字段仍放在 `env` 中：

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `LLM_TRACE_MODE` | `off` | `off` 或 `full`；显式开启时保存压缩的完整 LLM 决策 trace |
| `LLM_TRACE_AGENT_IDS` | 空 | 可选 Agent ID 逗号列表；空表示所有 Agent |
| `LLM_TRACE_TICKS` | 空 | 可选整数 tick 逗号列表；空表示所有 tick |

失败报告：

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `failure.json` | `<CHECKPOINT_DIR>/failure.json` | 一次 Runtime 最多写入一份致命 Failure 报告 |

Failure 报告独立于 checkpoint state，保存稳定错误字段、完整 traceback、异常 cause chain、
规范化 Effect 输入、执行上下文和可用的 LLM 决策证据。报告不做脱敏，完整保留
`runtime_config`（包括 API key 等凭据），供开发者排查。应按敏感文件管理 `failure.json`。
报告写入失败不会覆盖原始 Failure。


使用内置 `llm` kind 时必须显式配置 provider、workflow 和 role model。运行时不会选择默认
服务、模型或占位凭据。

网络 timeout、连接错误、HTTP 429 和 5xx 由 client 按 provider 的 `max_retries` 与
`retry_backoff_seconds` 指数退避。其他 4xx、响应 contract 错误和模型输出解析错误不做
网络重试。重试耗尽后抛出 `KernFailure` 并终止 runtime；不存在跨 tick cooldown。

`LLM_TRACE_MODE=full` 时输出：

```text
<CHECKPOINT_DIR>/llm_traces/index.jsonl
<CHECKPOINT_DIR>/llm_traces/tick_000001/<actor_id>/<trace_id>.json.gz
```

Trace 保存 perception、完整 messages、raw/parsed output 和 Action feedback，但不进入 checkpoint。
`llm_dialogue` trace 的 `completed` 表示模型已成功返回发言；对话 transcript 是否写入世界，
以同一运行的 `InteractionRecorded` / `ConversationCompleted` Event 和 interaction archive 为准。

## 10. Checkpoint 配置

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `CHECKPOINT_EVERY_TICK` | `1` | 是否启用运行档案 |
| `CHECKPOINT_INCLUDE_LOGS` | `1` | snapshot/delta 是否包含当前 tick 日志 |
| `CHECKPOINT_SNAPSHOT_INTERVAL_TICKS` | `60` | 完整 snapshot 间隔 |
| `CHECKPOINT_DIR` | 由 world data 路径派生 | 输出目录；建议在 config 中显式设置 |
| `CHECKPOINT_RESTORE_FILE` | 空 | 指定恢复文件 |
| `CHECKPOINT_RESTORE_DIR` | 空 | 指定恢复目录，取最新 archive snapshot |

只要显式配置了恢复文件或目录，路径不存在、路径类型错误或目录中没有 snapshot 都会
立即报错。恢复文件必须包含与当前运行时精确匹配的 `package_identity.v2`。

启用 checkpoint 写入时，`CHECKPOINT_DIR` 必须与恢复源目录不同。Runtime 会拒绝把新运行输出
写回恢复源，避免 ArchiveRecorder 初始化时覆盖原历史。若只做只读恢复检查，可设置
`CHECKPOINT_EVERY_TICK=0`。

启用后会写入：

- `manifest.json`
- `snapshots/snapshot_*.json.gz`
- `deltas/deltas_*.jsonl.gz`
- `simulation_log.json`

创建新的 `ArchiveRecorder` 会清理目标目录中的旧 archive 输出。需要保留历史运行时，请使用不同的 `CHECKPOINT_DIR`。

通过 `KernRuntime.from_config()` 装配且未显式设置 `CHECKPOINT_DIR` 时，默认路径由
`<project_root>/checkpoints/<manifest data.world>` 派生。直接构造 `KernRuntime` 时才回退到
当前工作目录的 `checkpoints/`。显式相对路径按当前工作目录解析；可复现实验应始终设置独立
输出目录。

## 11. TaskPolicy

`TaskPolicy` 不属于 runtime config，而是写在 recipe 中：

```json
{
  "process": {
    "duration": {"mode": "fixed", "value": 6},
    "assign_to": "self",
    "task_policy": {
      "interrupt_mode": "pause_keep_progress",
      "allow_voluntary_interrupt": true,
      "allow_voluntary_cancel": true
    }
  }
}
```

支持字段：

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `interrupt_mode` | `pause_keep_progress` | `forbidden`、`pause_keep_progress`、`pause_reset_progress`、`cancel`、`fail` |
| `allow_voluntary_interrupt` | `true` | 是否允许 agent 主动中断 |
| `allow_voluntary_cancel` | `true` | 是否允许 agent 主动取消 |

感知层只暴露必要摘要，例如自己的 `vitals`、当前任务是否可中断/取消，不默认暴露其他 agent 的内部体征。
